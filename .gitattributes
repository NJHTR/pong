# Evidence artifacts are byte-addressed records. Preserve captured terminal
# bytes, JSON formatting, exit-code files, and manifest bytes across checkouts;
# their SHA-256 references must describe the bytes CI actually reads.
artifacts/** -text -diff
