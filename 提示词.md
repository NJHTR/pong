# Pong 下一阶段 Codex 提示词：仓库代际迁移与 M1 FI-12

你现在继续维护 `D:\pong` 中的 Pong 项目。请先阅读：

- `docs/roadmap/NEXT_TASK.md`
- `docs/development/M1_DURABLE_PRIMITIVES_GATE.md`
- `docs/development/M1_EVIDENCE.md`
- `docs/protocol/VERSIONING_PROTOCOL.md`
- `docs/reliability/RECOVERY.md`
- `docs/architecture/STORAGE_ARCHITECTURE.md`
- `docs/decisions/ADR/ADR-0013-core-runtime-and-metadata-binding.md`
- `src/repository.rs`
- `src/metadata.rs`
- `tests/compatibility_boundaries.rs`
- `tests/migration_fixtures.rs`

## 当前项目定位

Pong 是面向 AI Agent 执行环境的 Git-like 版本控制与协调基础设施，不是 Multi-Agent Framework。当前只做 Rust durable primitives，不实现 CLI、Runtime interception、SDK、Server、UI 或框架适配器。

现有存储决策：

- Rust Core 内置 `rusqlite`，用户不需要单独安装 SQLite。
- SQLite/WAL/FULL 保存权威元数据、事件、refs、幂等记录和操作 journal。
- 文件系统 CAS 保存不可变对象。
- 工作区文件在 `.pong` 之外。
- 默认仓库格式是 `0.1`，marker 版本是 `1`，旧布局是 `.pong/metadata.sqlite` 加共享 CAS。
- 当前已有 canonical digest、CAS 原子发布/隔离、redaction、WAL、ref CAS、idempotency、unknown recovery、冷启动 failpoint、子进程 kill、WAL tail、全 `.pong` raw-byte scan 和 current/previous event-schema fixture 测试。

## 本次唯一目标

实现并验证真正的 **跨 SQLite 文件 repository-generation migration**，关闭 M1 的 `I-SCHEMA-02`、`I-SCHEMA-05`、`CT-08`、`FI-12` 缺口。

不要把“只在同一个 SQLite 文件内增加 `active_generation` 行”当作完整 repository-generation migration。跨文件切换的可见性必须由根 selector 决定。

## 必须采用的长期设计

### 1. 代际布局

保留旧库作为 legacy source，不在迁移前删除或覆盖：

```text
.pong/
  repository.json              # 唯一 active-generation selector
  metadata.sqlite              # legacy source；迁移后仍可保留，不能继续作为新代权威库
  objects/
  staging/
  quarantine/
  generations/
    <safe-generation-id>/
      metadata.sqlite
      generation.json
  migrations/
    <safe-migration-id>.json
  locks/
    repository.lock
```

`generation.json` 至少包含：generation id、repository format、schema version、storage driver、source marker digest、metadata digest/size、redaction profile id/version。所有字段都必须可验证、可重建、不能泄露 secret。

### 2. 兼容性与 selector

- 迁移后的 marker 必须升级 marker/repository/storage 兼容身份，例如 repository format `0.2`、marker version `2` 或等价明确方案。
- 旧 v0.1 reader 必须在任何 mutation 前返回 `UNSUPPORTED`，绝不能忽略 `active_generation` 后继续写旧 `metadata.sqlite`。
- 新 reader 要能识别 legacy v0.1 source，但普通 `open` 不得自动迁移；迁移必须是显式、离线操作。
- selector 是唯一 commit point：selector 切换前只有旧代可见，切换后只有新代可见。
- selector 损坏、active generation 缺失、manifest digest 不匹配、target 不完整时，返回 `RECOVERY_REQUIRED`/`INTEGRITY_ERROR`/`UNSUPPORTED`，绝不猜测或自动回退到另一代。

### 3. 迁移流程

实现一个内部/测试可用、未来可承载正式 API 的迁移入口，例如：

```rust
Repository::migrate(MigrationSpec) -> Result<MigrationOutcome, PongError>
Repository::open_with_migration_failpoints(...)
```

可以按项目现有风格命名，但必须保持职责清晰。流程至少包括：

```text
preflight
-> acquire offline writer/migration fence
-> allocate target generation
-> SQLite Online Backup API 完整复制 source
-> target schema/data transformation transaction
-> WAL checkpoint(TRUNCATE)，确认没有 BUSY
-> target integrity_check + foreign_key_check + format/schema/profile/refs/events/journal/CAS 语义验证
-> fsync target DB、sidecars、manifest、generation directory
-> selector pending
-> atomic selector replace
-> fsync selector/父目录
-> journal finalize（selector 已切换时只补记，不回滚）
```

硬性要求：

- 禁止直接复制 `metadata.sqlite` 而忽略 `-wal/-shm/-journal`；使用 SQLite Online Backup API。
- 迁移必须是明确 offline 操作。旧版本进程不认识迁移锁，因此不能假装 advisory lock 已经阻止旧 writer；至少取得 repository lock/独占 OS handle，并在文档中要求所有旧 Pong 进程关闭。
- selector 切换前的崩溃/磁盘满/权限失败：旧 selector 和旧代仍然是唯一可信可见状态；partial target 只能清理或隔离，不能被自动采用。
- selector 切换后的崩溃：以 selector 为准，不能自动回滚；目标损坏则报告 `RECOVERY_REQUIRED`/`QUARANTINED`。
- 迁移 id、generation id、路径必须拒绝空值、NUL、`..`、slash、reparse point/symlink/junction 等路径穿越。

### 4. 原子 selector 替换

- 临时 selector 必须写在 `repository.json` 同目录。
- 写完后 `sync_all`，再执行平台正确的原子替换。
- POSIX：fsynced temp + rename-overwrite + parent directory fsync。
- Windows：使用 `ReplaceFileW` 或等价的 `MoveFileEx(..., REPLACE_EXISTING | WRITE_THROUGH)`；禁止 `remove + rename`，禁止把 `hard_link` no-replace 当成替换方案。
- 替换后再次同步/验证；替换后的父目录同步失败属于 outcome-unknown，不得删除新 selector 或偷偷恢复旧 selector。

### 5. redaction 与扫描

迁移前后都要在所有 DB handle 关闭或受迁移锁保护时扫描完整 `.pong`：

- root metadata、WAL/SHM/journal
- `generations/`
- `migrations/`
- selector 临时文件、manifest、backup、staging、export、quarantine

扫描必须 fail-closed，错误文本不能包含 secret 或敏感路径。Windows reparse/symlink/junction 不得被递归跟随。

## 必须补的测试

不要只检查 fixture JSON 形状；必须实际运行迁移并冷重开检查磁盘状态。

至少新增/完成：

1. legacy v0.1 repository 迁移到 generation v0.2，事件 identity、opaque extension、refs、idempotency、journal 和 redaction profile 保持一致。
2. 迁移成功后普通 `Repository::open` 只打开 active generation；旧 `metadata.sqlite` 不再被新 reader 当成权威写入库。
3. 旧格式 reader 对 v0.2 selector 在 mutation/open 前返回 `UNSUPPORTED`。
4. failpoint 在 target allocation、backup 后、target checkpoint 后、target verify 后、selector replace 前、selector replace 后、selector directory sync 后分别触发；每个点 cold restart 后只能得到 old-only 或 new-only，不能 mixed generation。
5. selector replace 前失败重试可重建 target；source 发生变化时旧 target 必须丢弃/隔离而不能继续采用，避免丢写。
6. selector replace 后 journal 未 finalise 时重开应以 selector 为准并幂等补记，不回滚。
7. target manifest/DB/selector 任一损坏时 fail-closed，不自动猜测 active generation。
8. target disk-full、permission denied、CAS/redaction failure 时旧代仍可读；错误码稳定为 `RESOURCE_EXHAUSTED`、`PERMISSION_DENIED` 或 `INTEGRITY_ERROR`。
9. 重复相同 migration id/plan digest 幂等；同 id 不同 plan digest 拒绝。
10. 真实子进程 kill/cold restart；不要只用函数返回错误模拟崩溃。

fixture 可以继续使用：

- `tests/fixtures/CT-08-previous-repository-generation.json`
- `tests/fixtures/FI-12-migration-selector-interruption.json`

但要把 fixture 中的 selector 名称、repository format、generation manifest digest 与实际布局/实现对齐。 `tests/migration_fixtures.rs` 目前若只是 JSON 断言，不能算 FI-12 证据，必须增加真实 integration test。

## 文档与范围纪律

同步更新：

- `docs/architecture/STORAGE_ARCHITECTURE.md`
- `docs/protocol/VERSIONING_PROTOCOL.md`
- `docs/reliability/RECOVERY.md`
- `docs/decisions/ADR/ADR-0013-core-runtime-and-metadata-binding.md`，必要时新增 ADR-0014
- `docs/development/M1_DURABLE_PRIMITIVES_GATE.md`
- `docs/development/M1_EVIDENCE.md`
- `docs/roadmap/NEXT_TASK.md`
- `docs/roadmap/CHANGELOG.md`
- `tests/README.md`

明确区分：

- 已有的本地 synthetic failpoint/fixture evidence；
- 真正的 migration/cold-restart/platform evidence；
- 仍然缺失的 host-level quota/permission、property corpus、performance、supported-platform evidence。

不要实现 CLI、Runtime、SDK、Server、UI、workspace provider 或 agent framework adapter。

## 当前工作区特别提醒

当前正式代码中还没有完整的 repository-generation migration、`MigrationSpec`、generation selector 或 migration API，但工作区已经留下了一组**未收敛的底层准备改动**，必须先审计并继续完成，不能假设它们已经正确，也不要无故删回旧基线：

- `src/metadata.rs` 已有 crate-private 的 `begin_exclusive_migration`、`finish_exclusive_migration`、`checkpoint_truncate`、`backup_to` helper；检查事务/WAL/错误映射语义后再接入迁移流程。
- `Cargo.toml` 已加入 `fs2`、Windows `windows-sys`，并启用了 `rusqlite` 的 `backup` + `bundled` features。
- `Cargo.lock` 尚未与 `Cargo.toml` 同步；当前 `cargo check --locked` 会因 lockfile 需要更新而失败。先以正常 Cargo 流程更新 lockfile，再恢复全部 `--locked` 门禁。
- `src/metadata.rs` 当前还有 `cargo fmt --check` 差异；收敛实现时一并格式化。
- `src/repository.rs` 仍是 v0.1 单一 `.pong/metadata.sqlite` 布局，`repository.json` 也仍是初始化用 immutable marker；真正的 selector 解析、generation 布局、迁移编排和原子替换尚未实现。
- `tests/migration_fixtures.rs` 仍然只验证 JSON 形状，不能当作真实迁移或 FI-12 证据。

执行顺序：先审计并收敛现有草稿和依赖，设计/实现 migration contract，再补真实 integration/cold-restart 测试；完成后不得留下只为未来使用的 dead code。

- 不要留下 unused/dead-code 警告；
- 不要降低 clippy 标准；
- 不要把 fixture-only 测试写成已完成证据；
- 如果实现过程中遇到无法可靠完成的 Windows 原子替换、OS 锁或 WAL backup 语义，保留失败证据并更新 ADR/NEXT_TASK，不要用删除后重命名或复制 SQLite 主文件的临时替代方案。

## 最终质量门禁

在完成前必须执行并报告：

```powershell
cargo fmt --all -- --check
cargo check --locked
cargo test --locked
cargo clippy --locked --all-targets -- -D warnings
```

最终报告必须说明：

1. 实际实现了哪一个 migration contract；
2. selector 前后各故障点的 old/new-only 证据；
3. 哪些 M1 行现在有真实证据；
4. 哪些 M1 行仍未完成；
5. 当前 `NEXT_TASK.md` 指向的下一项工作。
